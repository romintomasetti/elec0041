{
    "build" : {
        "dockerfile" : "../dockerfile",
        "context"    : "../."
    },
    "extensions" : [
        "eamodio.gitlens",
        "mhutchie.git-graph",
        "analytic-signal.preview-pdf"
    ],
    "remoteEnv": {
        "DISPLAY" : "${localEnv:DISPLAY}"
    },
    "runArgs": [
        "--network=host",
    ]
}
